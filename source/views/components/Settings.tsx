import React from 'react';
import Container from '../../components/Container/Container';

type TProps = {};
type TState = {};

class Settings extends React.PureComponent<TProps, TState> {
    render() {
        return (
            <>
                Settings
            </>
        );
    }
}

export default Settings;
