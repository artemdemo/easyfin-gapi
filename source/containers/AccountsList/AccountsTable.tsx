import React from "react";
import {Column} from "react-table";
import {TAccountRowValues} from "../../google-api/GAccountRow";

type TProps = {
    columns: Column[];
    data: TAccountRowValues[];
};

const AccountsTable = (props: TProps) => {
    return null;
};

export default AccountsTable
